export enum NotifyEvent {
  scrollToTop = 'scrollToTop',
}

export enum SourceTokenCancel {
  CancelAction = 'CancelAction',
}

export enum KeyboardEventKey {
  Control = 'Control',
  Alt = 'Alt',
  Meta = 'Meta',
  Enter = 'Enter',
}
